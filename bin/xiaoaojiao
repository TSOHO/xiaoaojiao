#!/usr/bin/env node

const program = require('commander')

program
  .version(require('../package').version)
  .usage('<command>')
  .command('create', '从模板生成一个新项目').alias('c')
  .action(cmd => {
    if (cmd !== 'create' && cmd !== 'list') {
      program.outputHelp()
    }
  })

program.parse(process.argv)
